# Чеклист перед выкладкой на GitHub

Проверьте все пункты перед публикацией проекта:

## ✅ Файлы проекта

- [x] `README.md` — основная документация
- [x] `LICENSE` — лицензия MIT
- [x] `CONTRIBUTING.md` — руководство для контрибьюторов
- [x] `DEPLOY.md` — инструкция по выкладке
- [x] `.gitignore` — правильно настроен
- [x] `pyproject.toml` — метаданные проекта
- [x] `requirements.txt` — зависимости
- [x] `settings.ini` — конфигурация

## ✅ Очистка проекта

- [ ] Удалены все `__pycache__/` папки
- [ ] Удалены все `.pyc` файлы
- [ ] Удален `data.csv` (если содержит личные данные)
- [ ] Удалены обученные модели из `models/` (если есть)
- [ ] Удалены логи (`*.log`)

## ✅ Проверка кода

- [ ] Код работает без ошибок
- [ ] Тесты проходят: `pytest`
- [ ] Нет явных ошибок линтера
- [ ] Все импорты корректны

## ✅ Конфигурация

- [ ] В `pyproject.toml` обновлены ссылки на ваш GitHub репозиторий:
  ```toml
  [project.urls]
  homepage = "https://github.com/YOUR_USERNAME/robot-pose-helper"
  repository = "https://github.com/YOUR_USERNAME/robot-pose-helper"
  ```
- [ ] В `settings.ini` указаны разумные значения по умолчанию

## ✅ Git

- [ ] Инициализирован репозиторий: `git init`
- [ ] Все нужные файлы добавлены: `git add .`
- [ ] Сделан первый коммит: `git commit -m "Initial commit"`
- [ ] Добавлен remote: `git remote add origin https://github.com/YOUR_USERNAME/robot-pose-helper.git`
- [ ] Код отправлен: `git push -u origin main`

## ✅ GitHub

- [ ] Репозиторий создан на GitHub
- [ ] README отображается корректно
- [ ] Добавлены теги (topics): `robotics`, `pose-detection`, `mediapipe`, `python`
- [ ] Файлы на месте, лишние файлы не попали в репозиторий

## 📝 Команды для быстрой очистки (Windows PowerShell)

```powershell
# Удалить __pycache__
Get-ChildItem -Path . -Recurse -Filter "__pycache__" | Remove-Item -Recurse -Force

# Удалить .pyc файлы
Get-ChildItem -Path . -Recurse -Filter "*.pyc" | Remove-Item -Force

# Проверить что будет добавлено в Git
git status
```

## 📝 Команды для быстрой очистки (Linux/Mac)

```bash
# Удалить __pycache__
find . -type d -name "__pycache__" -exec rm -r {} +

# Удалить .pyc файлы
find . -type f -name "*.pyc" -delete

# Проверить что будет добавлено в Git
git status
```

---

После проверки всех пунктов следуйте инструкциям в [DEPLOY.md](DEPLOY.md).
