# Инструкция по выкладке проекта на GitHub

Это руководство поможет вам опубликовать проект на GitHub и подготовить его для других разработчиков.

## Подготовка проекта

### 1. Проверка файлов

Убедитесь, что в проекте есть следующие файлы:

- ✅ `README.md` — основная документация
- ✅ `LICENSE` — лицензия проекта (MIT)
- ✅ `CONTRIBUTING.md` — руководство для контрибьюторов
- ✅ `.gitignore` — список игнорируемых файлов
- ✅ `pyproject.toml` — метаданные и зависимости проекта
- ✅ `requirements.txt` — зависимости (для совместимости)
- ✅ `settings.ini` — конфигурационный файл

### 2. Очистка проекта

Перед коммитом удалите все временные файлы:

**Windows (PowerShell):**
```powershell
# Удалить все __pycache__
Get-ChildItem -Path . -Recurse -Filter "__pycache__" | Remove-Item -Recurse -Force

# Удалить все .pyc файлы
Get-ChildItem -Path . -Recurse -Filter "*.pyc" | Remove-Item -Force

# Удалить data.csv (если он есть и содержит ваши личные данные)
# Remove-Item -Path "data.csv" -Force
```

**Linux/Mac:**
```bash
# Удалить все __pycache__
find . -type d -name "__pycache__" -exec rm -r {} +

# Удалить все .pyc файлы
find . -type f -name "*.pyc" -delete

# Удалить data.csv (если нужно)
# rm -f data.csv
```

### 3. Проверка .gitignore

Убедитесь, что `.gitignore` содержит все необходимые паттерны. Проверьте, что следующие файлы/папки игнорируются:

- `__pycache__/`
- `*.pyc`, `*.pyo`
- `venv/`, `.venv/`
- `data.csv`
- `models/*.joblib`, `models/*.json`
- `.idea/`, `.vscode/`

## Создание репозитория на GitHub

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"New"** (или **"+"** → **"New repository"**)
3. Заполните форму:
   - **Repository name**: `robot-pose-helper` (или другое имя)
   - **Description**: "Сбор углов позы человека с камеры и обучение модели движений для роботов"
   - **Visibility**: выберите Public или Private
   - **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть в проекте)
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация Git в локальном проекте

Откройте терминал в корне проекта и выполните:

```bash
# Инициализация Git (если ещё не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: проект для сбора углов позы и обучения модели движений"

# Добавление remote репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/robot-pose-helper.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

### Шаг 3: Обновление pyproject.toml

После создания репозитория обновите ссылки в `pyproject.toml`:

```toml
[project.urls]
homepage = "https://github.com/YOUR_USERNAME/robot-pose-helper"
repository = "https://github.com/YOUR_USERNAME/robot-pose-helper"
issues = "https://github.com/YOUR_USERNAME/robot-pose-helper/issues"
```

Замените `YOUR_USERNAME` на ваш реальный GitHub username.

Затем сделайте коммит:

```bash
git add pyproject.toml
git commit -m "Обновлены ссылки на репозиторий в pyproject.toml"
git push
```

## Дополнительные настройки на GitHub

### 1. Добавление описания и тегов

На странице репозитория нажмите **"⚙️ Settings"** → **"General"** и добавьте:
- **Topics** (теги): `robotics`, `pose-detection`, `mediapipe`, `machine-learning`, `python`, `opencv`

### 2. Настройка README как главной страницы

GitHub автоматически отобразит `README.md` на главной странице репозитория. Убедитесь, что файл содержит:
- Описание проекта
- Инструкции по установке
- Примеры использования
- Информацию о лицензии

### 3. Создание Release (опционально)

Если хотите создать релиз:

1. Перейдите в **"Releases"** → **"Create a new release"**
2. Укажите версию: `v0.1.0`
3. Название: `Initial Release`
4. Описание: краткое описание функциональности
5. Нажмите **"Publish release"**

## Проверка после публикации

После выкладки проверьте:

- ✅ Репозиторий доступен по ссылке
- ✅ README.md отображается корректно
- ✅ Все файлы на месте (кроме игнорируемых)
- ✅ `.gitignore` работает правильно (нет лишних файлов)
- ✅ Ссылки в `pyproject.toml` ведут на ваш репозиторий

## Установка проекта другими пользователями

После публикации другие могут установить проект так:

```bash
# Клонирование репозитория
git clone https://github.com/YOUR_USERNAME/robot-pose-helper.git
cd robot-pose-helper

# Установка зависимостей
pip install -r requirements.txt
# или
pip install .
```

## Полезные команды Git

```bash
# Проверка статуса
git status

# Просмотр изменений
git diff

# Добавление изменений
git add .
git commit -m "Описание изменений"

# Отправка на GitHub
git push

# Обновление с GitHub
git pull
```

## Troubleshooting

**Проблема**: Git не видит некоторые файлы
- **Решение**: Проверьте `.gitignore` — возможно, файлы там указаны

**Проблема**: Ошибка при push
- **Решение**: Убедитесь, что вы авторизованы (`git config --global user.name` и `git config --global user.email`)

**Проблема**: Конфликт при push
- **Решение**: Сначала сделайте `git pull`, разрешите конфликты, затем `git push`

---

Удачи с проектом! 🚀
